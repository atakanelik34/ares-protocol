[build-system]
requires = ["setuptools>=69", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "ares-sdk"
version = "0.1.0"
description = "Python SDK for ARES Protocol"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
  "requests>=2.32.3",
  "pydantic>=2.9.2"
]
